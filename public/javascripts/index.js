

/*document.getElementById('formadd').addEventListener('submit', e => { 
    e.preventDefault();

    /*var inputlog = document.getElementById('content').value;
    var log = JSON.stringify([{ "content" : inputlog }]);
    console.log(inputlog);
    fs.writeFile('/data/logs.json', log , (err) => {
        if (err) throw err;
        console.log('saved');
    }); 
}); 

*/